# Bunny Render Control Lab

A dependency-free Python web service used to verify Render control and deployment.

The service listens on `0.0.0.0` at `PORT` (default `10000`) and exposes `/`, `/healthz`, and `/api/runtime`.
